# A5/Streams — Kafka Streams Starter

This folder contains notes and starter code for Kafka Streams projects.

Suggested mini-projects:
- Stateful counters with windowing and materialized state stores.
- Stream-table joins: enrich event streams with user profiles.

Starter files (to add):
- `src/main/java/.../StreamsApp.java` — minimal Spring Boot + Kafka Streams app.
- `docker-compose.streams.yml` — lightweight single-node Kafka for local testing.

Next steps for you:
- Create the `src` tree and add your Streams topology.
- Add tests using `TopologyTestDriver`.
