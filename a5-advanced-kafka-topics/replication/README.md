# A5/Replication — Multi-cluster & DR Starter

Notes and starter configs for replication and disaster recovery.

Suggested mini-projects:
- Configure MirrorMaker2 between two local clusters.
- Test failover and verify message parity.

Starter files (to add):
- `replication/mm2.properties` and `docker-compose.replication.yml`.

Next steps for you:
- Stand up two clusters and run MirrorMaker2 replication.
- Measure lag and observe boundary conditions.
