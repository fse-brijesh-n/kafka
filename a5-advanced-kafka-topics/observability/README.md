# A5/Observability — Monitoring & Tracing

Starter notes for monitoring Kafka and stream applications.

Suggested mini-projects:
- Export JMX metrics to Prometheus and import a Grafana dashboard.
- Add tracing (OpenTelemetry) to a Streams app.

Starter files (to add):
- `observability/prometheus.yml` and `observability/grafana/`.
- `observability/otel-config.yaml` for collectors.

Next steps for you:
- Deploy Prometheus + Grafana locally and import dashboards.
- Capture end-to-end traces for a sample stream flow.
